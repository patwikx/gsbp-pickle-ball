/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "res.cloudinary.com",
            "utfs.io",
            "4b9moeer4y.ufs.sh",
            "www.facebook.com"
        ]
    }
};

export default nextConfig;
